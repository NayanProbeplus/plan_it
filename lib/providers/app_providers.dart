
// /// Controls how many particles the splash should render.
// /// Tweak this number during development to test performance.
// final particleCountProvider = StateProvider<int>((ref) => 220);

// /// Toggle using Rive or the fallback animation.
// final useRiveProvider = StateProvider<bool>((ref) => true);
